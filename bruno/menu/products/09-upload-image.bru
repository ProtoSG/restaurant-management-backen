meta {
  name: Upload Product Image (ADMIN only)
  type: http
  seq: 9
}

post {
  url: {{base_url}}/products/{{product_id}}/image
  body: multipartForm
  auth: none
}

body:multipart-form {
  image: @file(./test-image.jpg)
}

assert {
  res.status: eq 200
  res.body.imageUrl: isDefined
}
