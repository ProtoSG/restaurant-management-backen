meta {
  name: Register User (ADMIN only)
  type: http
  seq: 2
}

post {
  url: {{base_url}}/auth/register
  body: json
  auth: none
}

body:json {
  {
    "name": "Juan Perez",
    "username": "juanperez",
    "password": "password123",
    "role": "WAITER"
  }
}

assert {
  res.status: eq 201
}
